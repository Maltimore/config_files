#!/bin/bash

cd ~
git init
git remote add origin https://github.com/Maltimore/dotfiles.git
rm .bashrc
git pull origin master

# remember to run :PluginInstall in vim to have vundle install the plugins


# install tmux
sudo apt-get install tmux
